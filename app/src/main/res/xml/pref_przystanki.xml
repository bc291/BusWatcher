<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <CheckBoxPreference
        android:defaultValue="false"
    android:key="show_minutes"
    android:summaryOn="Shown"
    android:summaryOff="Hidden"
    android:title="Czas odjazdu w minutach zamiast pełnej godziny"

    />
    <ListPreference
        android:defaultValue="@string/def_value_syncing_times"
        android:entries="@array/syncing_times"
        android:entryValues="@array/syncing_times_values"
        android:key="list_preference_1"
        android:title="Interwał synchronizacji odjazdów" />
</PreferenceScreen>